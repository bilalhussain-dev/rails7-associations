<p style="color: green"><%= notice %></p>

<h1>Products</h1>
<%= link_to "New product", new_product_path %>


<div id="products">
  <div class="row">
  <% @products.each do |product| %>
    <div class="col-md-4">
      <%= render product %>
    </div>
  <% end %>
  </div>
</div>

